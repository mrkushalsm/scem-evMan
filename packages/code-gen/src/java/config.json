{
  "language": "java",
  "boilerplate": "class Code {\n    public static void {method}({args}) {\n        // TODO: Implement solution\n        System.out.println(\"Output goes here\");\n    }\n}",
  "template": "import java.util.*;\nimport java.util.stream.*;\n\nclass Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n{input_reading_code}\n    }\n}\n\n{code}",
  "types": {
    "int": {
      "reader": "scanner.nextInt()",
      "hint": "int"
    },
    "float": {
      "reader": "scanner.nextFloat()",
      "hint": "float"
    },
    "char": {
      "reader": "scanner.next().charAt(0)",
      "hint": "char"
    },
    "string": {
      "reader": "scanner.next()",
      "hint": "String"
    },
    "int_array": {
      "reader": "int {var}_size = scanner.nextInt(); int[] {var} = new int[{var}_size]; for(int i=0; i<{var}_size; i++) { {var}[i] = scanner.nextInt(); }",
      "hint": "int[]"
    },
    "float_array": {
      "reader": "int {var}_size = scanner.nextInt(); float[] {var} = new float[{var}_size]; for(int i=0; i<{var}_size; i++) { {var}[i] = scanner.nextFloat(); }",
      "hint": "float[]"
    },
    "string_array": {
      "reader": "int {var}_size = scanner.nextInt(); String[] {var} = new String[{var}_size]; for(int i=0; i<{var}_size; i++) { {var}[i] = scanner.next(); }",
      "hint": "String[]"
    }
  }
}
