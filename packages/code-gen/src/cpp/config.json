{
  "language": "cpp",
  "boilerplate": "#include <bits/stdc++.h>\nusing namespace std;\n\nvoid {method}({args}) {\n    // TODO: Implement solution\n    cout << \"Output goes here\" << endl;\n}",
  "template": "#include <bits/stdc++.h>\nusing namespace std;\n\n{code}\n\nint main() {\n    ios_base::sync_with_stdio(false);\n    cin.tie(nullptr);\n{input_reading_code}\n    return 0;\n}",
  "types": {
    "int": {
      "reader": "cin >> {var};",
      "hint": "int"
    },
    "long": {
      "reader": "cin >> {var};",
      "hint": "long long"
    },
    "float": {
      "reader": "cin >> {var};",
      "hint": "double"
    },
    "double": {
      "reader": "cin >> {var};",
      "hint": "double"
    },
    "bool": {
      "reader": "cin >> {var};",
      "hint": "bool"
    },
    "char": {
      "reader": "cin >> {var};",
      "hint": "char"
    },
    "string": {
      "reader": "cin >> {var};",
      "hint": "string"
    },
    "int_array": {
      "reader": "int {var}_size; cin >> {var}_size; vector<int> {var}({var}_size); for (auto &e : {var}) cin >> e;",
      "hint": "vector<int>&",
      "selfDeclaring": true
    },
    "float_array": {
      "reader": "int {var}_size; cin >> {var}_size; vector<double> {var}({var}_size); for (auto &e : {var}) cin >> e;",
      "hint": "vector<double>&",
      "selfDeclaring": true
    },
    "string_array": {
      "reader": "int {var}_size; cin >> {var}_size; vector<string> {var}({var}_size); for (auto &e : {var}) cin >> e;",
      "hint": "vector<string>&",
      "selfDeclaring": true
    },
    "int_matrix": {
      "reader": "int {var}_rows, {var}_cols; cin >> {var}_rows >> {var}_cols; vector<vector<int>> {var}({var}_rows, vector<int>({var}_cols)); for (auto &r : {var}) for (auto &e : r) cin >> e;",
      "hint": "vector<vector<int>>&",
      "selfDeclaring": true
    }
  }
}
