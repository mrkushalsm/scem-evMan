{
  "language": "python",
  "boilerplate": "def {method}({args}):\n    # TODO: Implement solution\n    print(\"Output goes here\")",
  "template": "import sys\n\n{code}\n\nif __name__ == '__main__':\n    try:\n        input_data = sys.stdin.read().split()\n        iterator = iter(input_data)\n\n{input_reading_code}\n    except Exception as e:\n        sys.stderr.write(str(e))\n        sys.exit(1)",
  "types": {
    "int": {
      "reader": "int(next(iterator))",
      "hint": "int"
    },
    "long": {
      "reader": "int(next(iterator))",
      "hint": "int"
    },
    "float": {
      "reader": "float(next(iterator))",
      "hint": "float"
    },
    "double": {
      "reader": "float(next(iterator))",
      "hint": "float"
    },
    "bool": {
      "reader": "bool(int(next(iterator)))",
      "hint": "bool"
    },
    "char": {
      "reader": "next(iterator)",
      "hint": "str"
    },
    "string": {
      "reader": "next(iterator)",
      "hint": "str"
    },
    "int_array": {
      "reader": "[int(next(iterator)) for _ in range(int(next(iterator)))]",
      "hint": "list[int]"
    },
    "float_array": {
      "reader": "[float(next(iterator)) for _ in range(int(next(iterator)))]",
      "hint": "list[float]"
    },
    "string_array": {
      "reader": "[next(iterator) for _ in range(int(next(iterator)))]",
      "hint": "list[str]"
    },
    "int_matrix": {
      "reader": "{var}_rows = int(next(iterator)); {var}_cols = int(next(iterator)); {var} = [[int(next(iterator)) for _ in range({var}_cols)] for _ in range({var}_rows)]",
      "hint": "list[list[int]]",
      "selfDeclaring": true
    }
  }
}
