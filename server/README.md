# scem-evMan Server

## Setup

### Development

```bash
 docker compose -f docker-compose.dev.yaml up --build -d
```

### Production

```bash
 docker compose -f docker-compose.yaml up --build -d
```

